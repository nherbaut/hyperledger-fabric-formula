# -*- coding: utf-8 -*-
# vim: ft=yaml
---
hyperledger-fabric:
  pkg:
    name: hyperledger-fabric
  rootgroup: root
  config: '/etc/hyperledger-fabric'
  service:
    name: hyperledger-fabric
  subcomponent:
    config: '/etc/hyperledger-fabric-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
